{
  "name": "museo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "check:urls": "node scripts/check-urls.mjs",
    "sync:validation": "node scripts/sync-validation-status.mjs",
    "catalog:stats": "node scripts/catalog-inventory.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.0.0"
  }
}
